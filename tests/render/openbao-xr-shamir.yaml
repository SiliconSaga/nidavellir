# Offline-render fixture: the XRD default and the opt-out. A claim that says
# nothing about seal (today's committed claim) renders the manual Shamir
# posture; so does an explicit parameters.seal: shamir. Kept without the field
# so the render exercises the default path the live cluster is on until the
# operator graduates it.
apiVersion: nidavellir.siliconsaga.org/v1alpha1
kind: XOpenBao
metadata:
  name: openbao
spec:
  parameters:
    storageSize: "2Gi"
